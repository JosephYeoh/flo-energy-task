version: "3.9"
services:
  nem12:
    build: .
    image: nem12-meter-readings:latest
    command: ["--input-dir", "/samples", "--output-dir", "/data/out", "--logs-dir", "/data/_logs"]
    volumes:
      - ./data:/data
      - ./samples:/samples
